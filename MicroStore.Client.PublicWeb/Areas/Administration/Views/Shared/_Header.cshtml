
<nav class="main-header navbar navbar-expand  navbar-dark">
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button">
                <i class="fas fa-bars"></i>
            </a>
        </li>
    </ul>
    <ul class="navbar-nav ml-auto pl-5">
        <li class="nav-item ">
            <a class="nav-link disabled" data-widget="control-sidebar" data-slide="true"
               href="#">
               @if(Context.User.Identity?.IsAuthenticated ?? false){
                    @User.Claims.First(cl=> cl.Type == ClaimTypes.Name).Value
               }
            </a>
        </li>

        <li class="nav-item">
            <a href="#" class="nav-link" id="btn-logout">Logout</a>
            <form method="post" asp-area="" asp-controller="Authentication" asp-action="Logout" id="logout-form">
                @Html.AntiForgeryToken()
            </form>
        </li>
    </ul>
</nav>